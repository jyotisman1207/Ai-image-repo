Pixel Operations

Images are stored as numbers.

Each pixel has RGB values between 0 and 255.

Color inversion works by subtracting pixel values from 255.

Example:
New Pixel = 255 - Original Pixel

This creates the negative of an image.
