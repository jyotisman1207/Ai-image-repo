## Image Feature Extraction

In AI, images are converted into feature vectors.

A pretrained model like ResNet extracts important patterns such as:
- edges
- textures
- shapes

The output is a feature tensor that represents the image.

This is the first step in image captioning systems.
