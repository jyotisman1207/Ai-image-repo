## Contrast Stretching

Contrast stretching improves image contrast by expanding the range of pixel values.

Formula:
new_pixel = (pixel - min) * (255 / (max - min))

This makes dark pixels darker and bright pixels brighter.

It is useful in image enhancement and preprocessing.
