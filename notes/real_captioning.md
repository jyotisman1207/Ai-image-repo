## Real Image Captioning

Real image captioning uses deep learning models trained on large datasets.

This experiment uses a pretrained model (ViT + GPT2).

Pipeline:
- Vision Transformer (ViT) extracts image features
- GPT-2 generates text captions

This allows generating natural language descriptions of images.
